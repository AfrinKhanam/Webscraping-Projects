#---------------------------------------------------------------------#
def parse_ul_to_json(dom):
    #--------------------------------------#
    text = []
    for ele in dom.children:
        if ele.name is not None:
            text.append(ele.get_text())
    #--------------------------------------#

    return text
#---------------------------------------------------------------------#
